spring:
  datasource:
    url: jdbc:h2:mem:federation_db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true

server:
  port: 8082
